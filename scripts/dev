#!/bin/sh

(cd packages/auth.ui; yarn watch) &     \
(cd packages/auth.api; yarn watch) &    \
(cd packages/myinfo.ui; yarn watch) &  \
(cd packages/scanner; yarn watch) &     \
docker-compose up & wait
