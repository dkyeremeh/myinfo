apiVersion: apps/v1
kind: Deployment
metadata:
  name: recon-web
spec:
  selector:
    matchLabels:
      app: recon-web
  template:
    metadata:
      labels:
        app: recon-web
    spec:
      containers:
        - name: recon-web
          image: eldekyfin/recon.web
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: recon-web
spec:
  selector:
    app: recon-web
  ports:
    - port: 3000
      targetPort: 3000
